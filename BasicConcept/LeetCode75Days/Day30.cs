using SolrNet.Utils;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.Xml.Linq;

namespace BasicConcept.LeetCode75Days
{
    public class Day30
    {
        //public int KthLargestNumber(string[] nums, int kth)
        //{
        //    PriorityQueue<int, int> priorityQueue;

        //    Array.Sort(nums, (a,b)=> {
        //        if(a.Length == b.Length) a.CompareTo(b);
        //        return a > b;
        //    });

        //    //PriorityQueue
        //    return (int)long.Parse(nums[kth]);

        //   // return 0;
        //}

        //private bool Compare(string &X, string &y)
        //{
        //    if (x.Length != y.Length)
        //        return x.Length > y.Length;
        //    return x& < y&;
        //}
    }
}
